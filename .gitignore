# dependencies
/node_modules

# next.js
/.next/
/out/
next-env.d.ts

# misc
*.tsbuildinfo
.DS_Store

# extracted backup working files
/tmp/
*.sql
*.sql.gz

# Raw WordPress DB backup is kept OUT of the repo: it contains PII
# (user password hashes, emails, commenter IPs). Generated data/*.json is the
# committed, sanitised content instead.
*.zip
wpau-db-backuptwtDABDB.zip
